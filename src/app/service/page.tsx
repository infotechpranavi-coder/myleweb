import { LegalPage } from "@/components/ui/LegalPage";

export default function Page() {
  return (
    <LegalPage
      title="Service"
      body="Technical service, installation support and product identification are available through the AUTOPART contact channels."
    />
  );
}
